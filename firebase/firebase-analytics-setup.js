// firebase-analytics-setup.js
// This file contains the setup code for Firebase Analytics.
// It is currently disabled to avoid Content Security Policy (CSP) issues in the Chrome Extension environment.
// To enable Analytics:
// 1. Uncomment the import in firebase/firebase-config.js
// 2. Uncomment the initialization line in firebase/firebase-config.js
// 3. Ensure your manifest.json content_security_policy allows the Firebase Analytics domains.

/*
import { getAnalytics } from './firebase-analytics.js';

export function initAnalytics(app) {
  const analytics = getAnalytics(app);
  return analytics;
}
*/
